/**
 * Orb window chrome — presentation geometry only.
 * Not product config (see AGENTS.md: host / port / endpoint / model).
 */
export const ORB = Object.freeze({
  title: "Companion Orb",
  width: 240,
  height: 180,
  balloonHeight: 120,
});
